name: Deploy to VPS

on:
    push:
        branches:
            - main

jobs:
    main:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout code
              uses: actions/checkout@v3

            - name: Deploy to VPS
              uses: appleboy/ssh-action@v1.2.0
              with:
                  host: ${{ secrets.VPS_HOST }}
                  username: ${{ secrets.VPS_USER }}
                  key: ${{ secrets.VPS_SSH_KEY }}
                  port: 22
                  script: |
                      cd /domains/site-builder.dominikkoniarz.pl
                      git pull
                      export PATH=/root/.local/share/pnpm:$PATH
                      export PATH=/root/.local/share/pnpm/pnpm:$PATH
                      ./scripts/deploy.sh

#                       root@vmi2402461:/domains/site-builder.dominikkoniarz.pl# whereis pnpm
# pnpm: /root/.local/share/pnpm/pnpm
# root@vmi2402461:/domains/site-builder.dominikkoniarz.pl# sudo ln -s /root/.local/share/pnpm/pnpm /usr/local/bin/pnpm

# \node: /root/.local/share/pnpm/node
# root@vmi2402461:/domains/site-builder.dominikkoniarz.pl# whereis node
# node: /root/.local/share/pnpm/node
# root@vmi2402461:/domains/site-builder.dominikkoniarz.pl# sudo ln -s /root/.local/share/pnpm/node /use/local/bin/node
# ln: failed to create symbolic link '/use/local/bin/node': No such file or directory
# root@vmi2402461:/domains/site-builder.dominikkoniarz.pl# ls /usr/local/bin
# pnpm
# root@vmi2402461:/domains/site-builder.dominikkoniarz.pl# sudo ln -s /root/.local/share/pnpm/node /usr/local/bin/node
# root@vmi2402461:/domains/site-builder.dominikkoniarz.pl# whereis pm2
# pm2: /root/.local/share/pnpm/pm2
# root@vmi2402461:/domains/site-builder.dominikkoniarz.pl# sudo
