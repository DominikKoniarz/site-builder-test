export type CropData = {
    x: number;
    y: number;
    width: number;
    height: number;
};

export type RemovedBannerImage = {
    id: string;
    pageId: string;
    pageVariableId: string;
    imageName: string;
};
